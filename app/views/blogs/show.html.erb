<p style="color: green"><%= notice %></p>

<table class="table table-striped table-striped" style="width:80%;margin:auto" id="blogs">
<thead>
    <tr>
      <th scope="col">#</th>
      <th scope="col">Title</th>
      <th scope="col">Body</th>
      <th scope="col">user</th>
      <th scope="col">Tags</th>
    </tr> 
</thead>

<%= render @blog %>

</table>
<div>
  <% if session[:user_id].to_s == @blog.user %>
    <%= link_to "Edit this blog", edit_blog_path(@blog) %>
    <%= button_to "Destroy this blog", @blog, method: :delete %>
  <%end%>
  <%= link_to "Back to blogs", blogs_path %>
</div>
